{
  "compilerOptions": {
    "rootDir": "./src", /** carpeta de entrada; código TS*/
    "outDir": "./dist",  /* Carpeta de salida; código JS */
    "module": "es6",
    "target": "es6",
    "strict": true,
  },
  "include": ["src"],                 // Compilar solo lo que hay en /src
  "exclude": ["node_modules"]         // Ignorar completamente la carpeta node_modules cuando busque archivos .ts para compilar
}
